% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_map.R
\name{stac_map}
\alias{stac_map}
\title{Map STAC Item Footprints}
\usage{
stac_map(items)
}
\arguments{
\item{items}{A \link[tibble:tibble]{tibble::tibble} of STAC items as returned by
\code{\link[=stac_search]{stac_search()}} or \code{\link[=stac_items]{stac_items()}}, with a \code{geometry} list column.}
}
\value{
A \code{leaflet} htmlwidget object.
}
\description{
Creates an interactive 'leaflet' map showing the spatial footprints of
STAC items returned by \code{\link[=stac_search]{stac_search()}} or \code{\link[=stac_items]{stac_items()}}. Requires the
'leaflet' and 'sf' packages to be installed.
}
\examples{
\dontshow{if (has_internet() && requireNamespace("leaflet", quietly = TRUE) && requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
items <- stac_search(
  url = "https://earth-search.aws.element84.com/v1",
  collections = "sentinel-2-l2a",
  bbox = c(-84.5, 38.0, -84.3, 38.2),
  limit = 5
)
stac_map(items)
}
\dontshow{\}) # examplesIf}
}
