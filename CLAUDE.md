# CLAUDE.md — stacr

> Tidy STAC Workflows for R
> R >= 4.1.0 | Imports: rstac, tibble, cli

Read this file completely before doing anything.
Then read ai-dev/guardrails/ for hard constraints.

## CRAN Compliance (Non-Negotiable)

This package targets CRAN submission. Every change must:
- Pass `R CMD check --as-cran` with 0 errors, 0 warnings
- Have `@returns` on every exported function
- Have `@examples` or `@examplesIf` on every exported function
- Never write outside `tempdir()` in examples or tests
- Guard network-dependent examples with `@examplesIf`
- Use `\donttest{}` for slow examples, never `\dontrun{}`

## Package Structure

```
R/            — Source code (roxygen2 documented)
man/          — Generated by roxygen2 (do not edit)
tests/        — testthat edition 3
vignettes/    — Getting started guide
inst/extdata/ — Bundled sample data for examples
```

## Dependencies

- **Imports:** rstac, tibble, cli
- **Suggests:** gdalcubes, leaflet, sf, testthat, knitr, rmarkdown
- Use `requireNamespace("pkg", quietly = TRUE)` for Suggested packages

## Conventions

- roxygen2 with markdown enabled
- Google-style function documentation
- testthat edition 3
- All file I/O uses explicit path arguments
- All examples use bundled sample data or `tempdir()`
