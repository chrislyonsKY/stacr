% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_download.R
\name{stac_download}
\alias{stac_download}
\title{Download STAC Assets}
\usage{
stac_download(items, assets = NULL, output_dir = tempdir(), overwrite = FALSE)
}
\arguments{
\item{items}{An rstac \code{doc_items} object as returned by \code{\link[=stac_search]{stac_search()}} or
\code{\link[=stac_items]{stac_items()}} with the \code{raw} attribute, or the raw rstac result directly.}

\item{assets}{Character vector. Asset names to download (e.g.,
\code{c("red", "green", "blue")}). If \code{NULL}, downloads all assets.}

\item{output_dir}{Character. Directory where files are saved.
Defaults to \code{\link[=tempdir]{tempdir()}}.}

\item{overwrite}{Logical. Overwrite existing files? Defaults to \code{FALSE}.}
}
\value{
The rstac items object (invisibly) with assets downloaded to
\code{output_dir}. Downloaded file paths can be retrieved with
\code{\link[rstac:assets_functions]{rstac::assets_url()}}.
}
\description{
Downloads assets from STAC items returned by \code{\link[=stac_search]{stac_search()}} or
\code{\link[=stac_items]{stac_items()}}. Wraps \code{\link[rstac:assets_functions]{rstac::assets_download()}} with tidy output.
}
\examples{
\dontshow{if (has_internet()) withAutoprint(\{ # examplesIf}
\donttest{
# Search for items first
items <- stac_search(
  url = "https://earth-search.aws.element84.com/v1",
  collections = "sentinel-2-l2a",
  bbox = c(-84.5, 38.0, -84.3, 38.2),
  limit = 1
)
}
\dontshow{\}) # examplesIf}
}
