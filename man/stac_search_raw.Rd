% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_download.R
\name{stac_search_raw}
\alias{stac_search_raw}
\title{Search STAC and Return Raw rstac Result}
\usage{
stac_search_raw(
  url,
  collections = NULL,
  bbox = NULL,
  datetime = NULL,
  limit = 100L
)
}
\arguments{
\item{url}{Character. Root URL of a STAC API endpoint
(e.g., \code{"https://earth-search.aws.element84.com/v1"}).}

\item{collections}{Character vector. Collection IDs to search within.}

\item{bbox}{Numeric vector of length 4: \code{c(xmin, ymin, xmax, ymax)}.
Only items intersecting this bounding box are returned.}

\item{datetime}{Character. Date/time filter as a single datetime or
range (e.g., \code{"2024-01-01/2024-12-31"}). Follows RFC 3339.}

\item{limit}{Integer. Maximum number of items to return. Defaults to 100.}
}
\value{
An rstac \code{doc_items} object.
}
\description{
Like \code{\link[=stac_search]{stac_search()}} but returns the raw rstac \code{doc_items} object
instead of a tibble. Useful as input for \code{\link[=stac_download]{stac_download()}} and
\code{\link[=stac_to_cube]{stac_to_cube()}}.
}
\examples{
\dontshow{if (has_internet()) withAutoprint(\{ # examplesIf}
\donttest{
raw <- stac_search_raw(
  url = "https://earth-search.aws.element84.com/v1",
  collections = "sentinel-2-l2a",
  bbox = c(-84.5, 38.0, -84.3, 38.2),
  limit = 1
)
}
\dontshow{\}) # examplesIf}
}
