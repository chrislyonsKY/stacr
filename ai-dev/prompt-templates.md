# Prompt Templates — stacr

## Implementation
```
Read CLAUDE.md and ai-dev/architecture.md.
Implement [function]. Must include @returns and @examples.
Examples must use bundled sample data or tempdir().
Run devtools::check() before marking complete.
```

## CRAN Prep
```
Read CLAUDE.md and ai-dev/guardrails/cran-compliance.md.
Run R CMD check --as-cran. Fix all warnings and notes.
Verify all @examples run in < 5 seconds.
Verify no writes outside tempdir().
```
